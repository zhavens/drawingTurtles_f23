import turtle
import settings as L

def draw():
    starting_pos = L.turtle.pos()
    L.back(20)
    L.right(90)
    L.forward(10)

    L.turtle.penup()
    L.turtle.setpos(starting_pos)
    l.turtle.setHeading(0)
    L.turtle.pendown() 

draw()                 
    
